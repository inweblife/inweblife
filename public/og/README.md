OG assets policy

- Files in this folder are derived social preview images (Open Graph / Twitter).
- Keep source/original visuals in `public/` unchanged.
- If OG images need updates, regenerate only files in `public/og/`.
- Do not overwrite original `.webp` source files in `public/`.
